#include "SlotRecCustomType.h"

namespace Greis
{
    SlotRecCustomType::SlotRecCustomType( char* p_message, int p_length ) 
        : _size(p_length)
    {
        // ${DeserializationConstructorStub}
    }

    QByteArray SlotRecCustomType::ToByteArray() const
    {
        QByteArray result;

        // ${ToByteArrayStub}
        
        return result;
    }
}
