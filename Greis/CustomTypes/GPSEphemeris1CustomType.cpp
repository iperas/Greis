#include "GPSEphemeris1CustomType.h"

namespace Greis
{
    GPSEphemeris1CustomType::GPSEphemeris1CustomType( char* p_message, int p_length ) 
        : _size(p_length)
    {
        // ${DeserializationConstructorStub}
    }

    QByteArray GPSEphemeris1CustomType::ToByteArray() const
    {
        QByteArray result;

        // ${ToByteArrayStub}
        
        return result;
    }
}
