#include "GPSAlm1CustomType.h"

namespace Greis
{
    GPSAlm1CustomType::GPSAlm1CustomType( char* p_message, int p_length ) 
        : _size(p_length)
    {
        // ${DeserializationConstructorStub}
    }

    QByteArray GPSAlm1CustomType::ToByteArray() const
    {
        QByteArray result;

        // ${ToByteArrayStub}
        
        return result;
    }
}
