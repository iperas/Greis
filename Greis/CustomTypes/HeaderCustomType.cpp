#include "HeaderCustomType.h"

namespace Greis
{
    HeaderCustomType::HeaderCustomType( char* p_message, int p_length ) 
        : _size(p_length)
    {
        // ${DeserializationConstructorStub}
    }

    QByteArray HeaderCustomType::ToByteArray() const
    {
        QByteArray result;

        // ${ToByteArrayStub}
        
        return result;
    }
}
